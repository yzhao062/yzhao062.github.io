name: Lighthouse CI

on:
  pull_request:
  push:
    branches:
      - main
    # meta-finder pushes citation counts here every couple of days. No page
    # reads that file, so a performance audit on it measures nothing.
    paths-ignore:
      - 'data/citations.json'
  workflow_dispatch:

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Start local static server
        run: |
          python3 -m http.server 8080 --directory . >/tmp/http-server.log 2>&1 &
          sleep 2

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          configPath: ./.lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
